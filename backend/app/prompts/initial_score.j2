基于候选人简历与岗位 JD 的匹配度，给出一个"印象分"作为面试起点（范围 {{ min_score }}-{{ max_score }}），并必须按照以下 5 个维度给出**逐项依据**——这是为了让候选人在复盘时能清楚地知道"为什么是这个分"。

# 简历
{{ resume_text[:2500] if resume_text else "（候选人未上传简历）" }}

# 岗位
标题：{{ job_title }}
{{ job_jd[:1500] if job_jd else "" }}

# 评分维度（每项 1-10 分，必须给出 reason）
1. **education** —— 学校层级（顶尖 985/海外名校 / 普通 985/211 / 双非）+ 最高学历（博士/硕士/本科/大专）
2. **experience** —— 大厂经验（腾讯、字节、阿里、Google 等）+ 工作年限 + 角色级别
3. **projects** —— 项目经验质量（是否有可量化产出、技术深度、规模）
4. **papers** —— 论文 / 顶会 / 专利 / 开源 / 技术博客等学术或社区影响力
5. **match** —— 简历技能与岗位 JD 的匹配度（必须列出共同关键词）

# 输出严格 JSON（不要 markdown 代码块）：
{
  "score": <int，{{ min_score }}-{{ max_score }} 之间，是 5 维加权后的整体印象分>,
  "reason": "<一句 30 字以内的总结>",
  "breakdown": {
    "education": {"score": <1-10>, "reason": "<解释，要点出具体学校/学历>"},
    "experience": {"score": <1-10>, "reason": "<解释，要点出具体公司/年限>"},
    "projects": {"score": <1-10>, "reason": "<解释，要列出 1-2 个项目亮点>"},
    "papers": {"score": <1-10>, "reason": "<解释，列出论文/开源/专利；没有就如实写没有>"},
    "match": {"score": <1-10>, "reason": "<解释，列出 JD 中匹配的关键词；不匹配也如实写>"}
  }
}

注意：
- 如果某个维度信息缺失（例如简历未提学校），给中等分（5-6）并说明"信息不足"，不要凭空编造。
- reason 必须是中文、可被候选人直接看懂的"为什么"，不要给"良好""一般"这种空泛词。
